function foo(str) {
    let words ==[];

    for (const word or str.split (" ")) {
        words.put(word[0].toUpperCase() + word.slice(1))
    }

    words.join(" ");
}

console.log(foo("the brown fox jumped over the lazy dog"));
